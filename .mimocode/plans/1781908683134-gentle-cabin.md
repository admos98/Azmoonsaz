# Fix Plan — Azmoonsaz Codebase Issues

## Issues Found

| # | Severity | Issue | File(s) |
|---|----------|-------|---------|
| 1 | CRITICAL | Missing `Settings.tsx` — build fails | `src/pages/teacher/Settings.tsx` (create) |
| 2 | HIGH | `isSecureTeacherModeAvailable()` ignores `enableMockMode` | `src/services/teacherApi.ts:5-7` |
| 3 | HIGH | `classService` never calls backend + API only has GET | `src/services/api.ts:115-143`, `api/index.js` |
| 4 | MEDIUM | `Student` type missing `status` field, `as any` casts | `src/types.ts:12-21`, `src/pages/teacher/Students.tsx` |
| 5 | LOW | Debug `console.log` left in production code | `src/services/offlineAnswerQueue.ts:41,48` |
| 6 | LOW | Mock exam code 5 chars vs API 6 chars | `src/services/api.ts:317` |

## Fix 1: Create `src/pages/teacher/Settings.tsx`

Create a system settings page matching existing page patterns (Dashboard, Students). Sections:
- **Teacher profile** — display name/school/email with inline edit (localStorage in mock mode)
- **Runtime status** — backend mode, Supabase config status, mock mode indicator
- **App info** — system name, version, framework, hosting

Uses: `Card`, `Button`, `Input`, `Badge` from UIComponents; `lucide-react` icons; `motion/react`; `authService`; `isSecureBackendMode()`/`getRuntimeModeLabel()` from runtimeMode; `publicEnv` from config/env. All Persian text, RTL layout.

## Fix 2: `isSecureTeacherModeAvailable()` — `src/services/teacherApi.ts`

**Line 6**: `return publicEnv.isSupabaseConfigured;` → `return publicEnv.isSupabaseConfigured && !publicEnv.enableMockMode;`

This matches `isSecureBackendMode()` from runtimeMode.ts. Without this fix, mock mode with Supabase credentials causes real API calls that fail.

## Fix 3: `classService` backend integration

### 3a: Extend `handleTeacherClasses` in `api/index.js` (line 471-478)
Change from GET-only to GET+POST with create/update/delete actions, following the exact pattern of `handleTeacherStudents`.

### 3b: Update `classService` in `src/services/api.ts` (lines 115-143)
Add `isSecureTeacherModeAvailable()` checks + `teacherGet`/`teacherPost` calls, matching the pattern used by `studentService`, `questionService`, `examService`.

## Fix 4: Add `status` to `Student` type

### 4a: `src/types.ts` line 20 — add `status?: 'active' | 'suspended' | 'examining';`
### 4b: `src/pages/teacher/Students.tsx`
- Delete `StudentWithStatus` interface (lines 37-40)
- Replace all `StudentWithStatus` → `Student`
- Remove `(s as any).status` cast (line 54) → `s.status`

## Fix 5: Remove debug logs — `src/services/offlineAnswerQueue.ts`

Delete `console.log` at lines 41 and 48. Keep `console.warn` (legitimate error handling).

## Fix 6: Mock exam code length — `src/services/api.ts`

Line 317: change `for (let i = 0; i < 5;` → `for (let i = 0; i < 6;` to match API's 6-char codes.

## Execution Order

1. Fix 5 (console.log removal) — trivial
2. Fix 6 (exam code length) — trivial
3. Fix 2 (isSecureTeacherModeAvailable) — prerequisite for Fix 3
4. Fix 4 (Student type + Students.tsx cleanup)
5. Fix 3a (api/index.js class route extension)
6. Fix 3b (classService backend calls)
7. Fix 1 (Settings.tsx creation) — largest change
8. Run `npm run lint` to verify type correctness

## Verification

- `npm run lint` passes (tsc --noEmit)
- `npm run build` succeeds
- Settings page renders when clicking "تنظیمات سامانه" in sidebar
- All service methods correctly fall back to localStorage in mock mode
