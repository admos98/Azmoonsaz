/**
 * Central mock-data barrel.
 *
 * AI Studio generated several imports from src/mockData.
 * The real mock data lives under src/data.
 */

export { mockTeacher, mockTeachers } from './data/mockTeachers';
export { mockStudents, mockClassGroups } from './data/mockStudents';
export { mockQuestions } from './data/mockQuestions';
export { mockExams } from './data/mockExams';
export { mockSubmissions } from './data/mockSubmissions';
